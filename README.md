## Tasks management app demo built with MEAN

### Resources

- Angular 6
- Express
- Mongoose
- NodeJS

### Usage

1. Install dependencies: `yarn install` | `npm install`
2. Run application on browser: `yarn start` | `npm start`
